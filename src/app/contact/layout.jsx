import React from 'react'

export const metadata ={
  title: 'Contact Us',
  description: 'We are your best reach',
}

export default function layout({children}) {
  return (
    <div>
    {children}
    </div>
  )
}
